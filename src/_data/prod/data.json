{
  "id": "{{ id }}",
  "number": "{{ number }}",
  "name": "{{ name }}",
  "subtitle": "{{ subtitle }}",
  "addressStreet": "{{ addressStreet }}",
  "addressLocality": "{{ addressLocality }}",
  "addressRegion": "{{ addressRegion }}",
  "addressCountry": "{{ addressCountry }}",
  "postalCode": "{{ postalCode }}",
  "phone": "{{ phone }}",
  "email": "{{ email }}",
  "website": "{{ website }}",
  "cuisine": "{{ cuisine }}",
  "url": "{{ url }}",
  "openingTimes": "{{ openingTimes }}",
  "openingNotes": "{{ openingNotes }}",
  "geocode": {
    "lat": "{{ geocode.lat }}",
    "lng": "{{ geocode.lng }}"
  },
  "sameAs": "{{ social.sameAs }}",
  "social": {
    "sameAs": "{{ social.sameAs }}",
    "networks": "---------------[[ INSERT SOCIAL LOOP ]]---------------"
  },
  "image": {
    "default": "{{ image.default }}",
    "opengraph": "{{ image.default }}",
    "gallery": "---------------[[ INSERT IMAGE GALLERY LOOP ]]---------------"
  },
  "description": {
    "sentence": "{{ description.sentence }}",
    "paragraph": "{{ description.paragraph }}",
    "full": "{{ description.full }}"
  },
  "features": "-----------------[[ INSERT FEATURES LOOP ]]--------------------",
  "transport": "{{ transport }}",
  "parking": "{{ parking }}",
  "menus": {
    "overview": "{{ menus.overview }}",
    "sections": "---------------[[ INSERT MENU SECTION LOOP ]]---------------",
    "pdfs": "--------------[[ INSERT PDF MENU LOOP ]]---------------"
  },
  "reservations": {
    "info": "{{ reservations.info }}",
    "groups": "{{ reservations.groups }}",
    "private": "{{ reservations.private  }}",
    "tgtUrl": "{{ reservations.tgturl }}",
    "maxcover": "{{ reservations.maxcover }}",
    "provider": "{{ reservations.provider }}",
    "restId": "{{ reservations.rest_id }}",
    "bkgAdvDays": "{{ reservations.bkgAdvDays }}"
  },
  "config": {
    "theme": "apptiser",
    "domain": ""
  },
  "providers": {
    "serb": {
      "widgetUrl": "{{ serb Widget Url }}"
    },
    "mozrest": {
      "id": "{{ Mozrest Id }}",
      "key": "{{ Mozrest Key }}"
    }
  }
}

