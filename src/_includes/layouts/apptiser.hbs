
<!DOCTYPE html>
<html lang="{{ language }}" data-id="{{ apptiser.number }}" xmlns="http://www.w3.org/1999/html">

<!-- {{ pkg.version }} -->

<head>

	<meta charset="utf-8">
    <title>{{ data.name }} | {{ data.streetAddress }} {{ data.postalCode }}</title>
    <meta name="description" content="{{ data.description.sentence }}">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- TODO: should only be applied to previews -->
<!--    <meta name="robots" content="noindex">-->

    {{> partials/schema.org }}

    {{> partials/open-graph }}

    <link rel="icon" href="{{ config.assets }}/imgs/favicon.svg">
    <link rel="apple-touch-icon" href="{{ config.assets }}/imgs/apple-touch-icon.png">
    <link rel="manifest" href="{{ config.assets }}/manifest.json">

    {{> partials/fonts }}

    {{#if data.theme}}
        <link rel="stylesheet" href="{{ config.assets }}/styles/themes/{{ data.theme }}.css" />
    {{/if}}

	<link rel="stylesheet" href="{{ config.assets }}/styles/member.css" />

	<script type="module" src="{{ config.assets }}/scripts/main.js" defer></script>

    {{> partials/google-apis }}

</head>

<body>

	{{{ content }}}

</body>
</html>
